# Antigravity в России без VPN и смены региона аккаунта Google.

Программа для windows, которая позволяет аккаунту Google любой страны (в том числе России) использовать Antigravity.

Видеоинструкция: https://youtu.be/mQvnQkua734

Процедура разблокировки проводится один раз для компьютера. После этого можно авторизовываться с любыми аккаунтами Google из любых стран.


# Где скачать?

Нажимаете на [Releases](https://github.com/confeden/Antigravity/releases) и скачиваете последнюю версию.

Для разблокировки нужно ввести бесплатный лицензионный код, взять можно из группы (в закреплённых сообщениях) [t.me/nova_txt](https://t.me/nova_txt/69864)

# Как разблокировать Antigravity?
1) скачиваете Antigravity/Antigravity IDE/Antigravity CLI с официального сайта [https://antigravity.google/download](https://antigravity.google/download) и устанавливаете
2) применяете анлокер
3) запускаете Antigravity и входите в аккаунт Google

# Работает ли анлокер для 2+ версии?
Да, он работает для всех агентских программ для кодинга от Google:
- Antigravity 2.0
- Antigravity IDE
- Antigravity CLI
- Gemini CLI

# Что именно анлокер меняет в системе?

Ровно три вещи, всё обратимо (пункт меню «Полный откат»):

1. **Бинарник Language Server / CLI** (`language_server.exe`, `agy.exe`) — переименовывается одно поле в protobuf-дескрипторе (`ineligible` → `inexigible`). Длина строки та же, размер и структура файла не меняются.
2. **NRPT-правила DNS** — только 4 конкретных хоста, к которым обращается сам Antigravity, направляются на встроенный DNS-резолвер (по умолчанию — гонка штатных провайдеров: `geohide` подменяет `daily-cloudcode-pa.googleapis.com`):
   `cloudcode-pa.googleapis.com`, `daily-cloudcode-pa.googleapis.com`, `generativelanguage.googleapis.com`, `antigravity-unleash.goog`.
   Остальной DNS-трафик (в том числе `accounts.google.com` и прочие `*.googleapis.com`) идёт через ваш обычный резолвер.
3. **Приоритет IPv4 над IPv6** — применяется только если на машине нет глобального IPv6-адреса, и снимается при откате.

> **Важно (актуально с 2026):** часть штатных резолверов (в т.ч. `xbox-dns.ru`)
> перестала подменять IP для хостов Antigravity, поэтому разблокировка по DNS
> перестала работать (ошибка `400 FAILED_PRECONDITION: User location is not supported`).
>
> **Решено в этой сборке двумя слоями:**
> - штатный фикс: CloudCode направляется на `daily-cloudcode-pa.googleapis.com`,
>   который подменяет `geohide.ru` (входит в гонку провайдеров по умолчанию);
> - переключатель резолвера в меню — `9. Сменить резолвер`: можно выбрать рабочий
>   Smart-DNS `Malw.link` (`193.23.209.189`) или любой другой, а также задать
>   свой адрес (пункт `8. Сменить адрес DNS-резолвера`).
>
> `Malw.link` НЕ является дефолтным — это один из пресетов переключателя. Чтобы
> использовать именно его, выберите пункт 9 → `malw.link` (сохраняется в переменные
> окружения, без пересборки).
>
> Переопределить резолвер можно и вручную через переменные окружения
> (приоритетнее штатного набора):
> - `AG_UNLOCKER_DNS` — список IPv4 через запятую, например `1.2.3.4,5.6.7.8`;
> - `AG_UNLOCKER_DNS6` — список IPv6 через запятую.
> Или пункт меню `8. Сменить адрес DNS-резолвера` (сохраняет адрес в переменные
> окружения, не в файл).
>
> Рабочий DNS — это тот, что отдаёт IP разрешённого региона для
> `cloudcode-pa.googleapis.com` / `daily-cloudcode-pa.googleapis.com` и др.
> Свежий адрес обычно дают в группе `t.me/nova_txt`.

Файлы `app.asar` начиная с Antigravity 2.4 не распаковываются и не изменяются вообще.

# На virustotal есть срабатывания. Почему?

Антивирусы (в особенности бесплатные) любят тревожиться, когда не могут найти известные сигнатуры файла, когда у него отсутствует платная подпись и просто тогда, когда их содержимое сложнее, чем обычно. Исходный код открыт для анализа, но если вы не доверяете - используйте его на виртуальной машине.

🙋 Группа в ТГ: [t.me/nova_txt](https://t.me/nova_txt)
